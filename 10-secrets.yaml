- name: Demo on ansible vault
  hosts: all
  vars:
    USERNAME: sqlroot
    PASSWORD: robo@1
  tasks: 
    - name: Printing the output
      ansible.builtin.debug:
        msg: "Printing the password {{ PASSWORD }}"