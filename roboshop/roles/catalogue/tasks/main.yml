- name: "Disable existing nodejs module stream for {{ component }} server"
	ansible.builtin.shell: dnf module disable nodejs

- name: "Enable nodejs module stream 20"
	ansible.builtin.shell: dnf module enable nodejs:20

- name: "Install nodejs package"
	ansible.builtin.dnf:
		name: nodejs
		state: present